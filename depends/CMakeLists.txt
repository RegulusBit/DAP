file(GLOB_RECURSE SRCS *.cpp)
file(GLOB_RECURSE HDRS *.h)
file(GLOB_RECURSE SRCS2 *.c)
file(GLOB_RECURSE HDRS2 *.hpp)



add_library(secp256k1 ${SRCS} ${HDRS} ${SRCS2} ${HDRS2})

SET_TARGET_PROPERTIES(secp256k1 PROPERTIES LINKER_LANGUAGE CXX)